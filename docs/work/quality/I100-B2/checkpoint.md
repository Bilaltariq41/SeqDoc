# I100-B2 bounded process cleanup and Restart Manager attribution

## State

`Blocked`

This package becomes immutable only at its commit SHA. It is planning-only, with baseline explicitly provisional `18aae5e0364c0b13549bd0c5333ba48f8116bc9a`. Issue [#117](https://github.com/Bilaltariq41/SeqDoc/issues/117) depends on GH-116. Before `Ready`, amend this baseline and dependency receipt to the exact accepted GH-116 merge/head and handoff claims; no owner authorization is inferred. Consume only accepted GH-106 head `182ea35533482cebdfc070b368f3a7fa247a1735`, merge `227d2e9f8b49ce6a414795b16bb0408ed213012a`.

## Allowlist, non-goals, and inherited contract

Initial authored implementation is only `tests/SeqDoc.AcceptanceTests/FixtureCleanup.cs` and `FixtureCleanupTests.cs`; `SeqDoc.AcceptanceTests.csproj` is excluded and may be added only by a new peer-approved amendment proving wildcard/stub staging insufficient. Capsule, ledger, canonical records, and projection through `work_state.py` are the only planning paths. No package/project/stub/product/workflow changes, PATH search, global kill/prune, unbounded wait, or parallel lease. B2 inherits B1's rooted Git, sentinel, FILE_ID, exact vector, and active-zero authority.

Authored package paths are this checkpoint, ledger, and `docs/project/work-items/GH-117.json`; generated `docs/project/execution.json` is expected unchanged and idle. Current-main planning baseline is exactly `18aae5e0364c0b13549bd0c5333ba48f8116bc9a`; no current-main test or acceptance result is claimed. Prospective activation claims, acquired only after GH-116 claims are released/closed, GH-116 is accepted, and review is complete: `path:tests/seqdoc.acceptancetests/fixturecleanup.cs`, `path:tests/seqdoc.acceptancetests/fixturecleanuptests.cs`, `path:docs/work/quality/i100-b2`, `path:docs/project/work-items/gh-117.json`, `fixture:fixturecleanup-process-rm`, `governance-tool:tools/governance/work_state.py`, `exclusive:acceptance-git-worktree-metadata`. No csproj claim.

## Frozen process/RM contract and risks

`outerDeadline = cleanupStart(monotonic) + CleanupTimeout + 10s FamilyProofGrace`; `CleanupTimeout` is >0 and <=30s. Requested `WaitAsync` is at most `max(0, remainingOuter-10s)`. Caller cancellation is primary evidence but cleanup has an independent token. Check remaining time before every process/gate/Git/RM/delete/native call; expiry starts no new stage. Dispose and opened `RmEndSession` are finally obligations that may overrun, recording class and overrun without duration.

The logical target is one receipt-listed role path. A direct delete/open violation is an `IOException` from an exact existing regular receipt-listed file with `(HResult & 0xffff)` 32 or 33; directory-only failures are not attribution. Attempt offsets are absolute from retry start. RM is invoked at most once per logical target on the first 32/33; failed Start/Register/GetList/End consumes the invocation. Existing regular files are sorted, local-only, maximum 32. RM immutable table: `Rstrtmgr.dll`, Unicode, `ExactSpelling=true`, `CallingConvention.Winapi`, `SetLastError=false`; constants `ERROR_SUCCESS=0`, `ERROR_MORE_DATA=234`, `CCH_RM_SESSION_KEY=32` with key capacity 33, `CCH_RM_MAX_APP_NAME=255` plus one (256 WCHAR), and `CCH_RM_MAX_SVC_NAME=63` plus one (64 WCHAR). `FILETIME` is two uint fields; `RM_UNIQUE_PROCESS` is PID plus FILETIME at offsets 0/4, size 12; `RM_PROCESS_INFO` fields are UNIQUE, 256-WCHAR app name, 64-WCHAR service name, signed int32 app type, uint status, uint session, Win32 bool at offsets 0,12,524,652,656,660,664, size 668. Exact signatures are `uint RmStartSession(out uint handle, uint flags, StringBuilder key)`, `uint RmRegisterResources(uint handle, uint nFiles, [In, MarshalAs(UnmanagedType.LPArray, ArraySubType=UnmanagedType.LPWStr, SizeParamIndex=1)] string[] files, uint nApps, IntPtr apps, uint nServices, IntPtr services)`, `uint RmGetList(uint handle, out uint needed, ref uint count, [In, Out, MarshalAs(UnmanagedType.LPArray, SizeParamIndex=2)] RM_PROCESS_INFO[] apps, out uint rebootReasons)`, and `uint RmEndSession(uint handle)`; apps/services are zero and `IntPtr.Zero`. The maximum is three `RmGetList` calls: null/zero first, one growth on `MORE_DATA`, third `MORE_DATA` fails; needed/count must be bounded at 64, malformed or non-growing results fail, no partial result is exposed, and successful Start always records End evidence. No `RmShutdown` declaration exists. UnauthorizedAccess retries only while authority remains valid and does not invoke RM absent 32/33.

Retry is frozen from retry-start: attempt starts are absolute `[0, 50, 150, 350, 750, 1150, 1550, 1950]` ms and inter-attempt delays are `[50, 100, 200, 400, 400, 400, 400]` ms. There are at most eight attempts. A nested monotonic deletion subdeadline of 2 seconds and the remaining outer deadline must both admit the next attempt; otherwise it is not started. Stop immediately on success, nonretryable failure, invalid authority, or deadline. Revalidate the full B1 authority before every attempt and after every awaited delay before mutation.

Risks are deadline/grace arithmetic, cancellation masking, post-expiry stage starts, family-zero races, retry boundary drift, stale PID attribution, RM malformed counts/caps/API leaks, directory misattribution, and primary-failure masking. Existing coverage is GH-106 76/76 and B1's three authority tests; no accepted process/RM contract exists.

## Tests and budget

Add exactly three `[Fact]` methods in `FixtureCleanupProcessTests`: (1) deadline, cancellation, reserved grace, finally obligations, and no-new-stage chronology; (2) exact retry offsets, 32/33/UnauthorizedAccess partitions and logical-target attribution; (3) RM admission, layouts, bounded state machine, malformed/cap/error/end cases, and primary/degradation precedence. Focused discovery is exactly `3 passed/0 failed/0 skipped`; unavailable Windows x64/Git/RM is failure, never skip. This is a soft budget of three grouped claims with no exception.

Process test 2 asserts exactly eight or fewer started attempts, the observed absolute start offsets and seven inter-attempt delays, no start beyond either budget, full authority revalidation before each attempt and after delay, and immediate stop partitions.

## Verification, review, and stop

Focused (not run in planning):
```powershell
dotnet test tests/SeqDoc.AcceptanceTests/SeqDoc.AcceptanceTests.csproj -c Release --filter FullyQualifiedName~FixtureCleanupProcessTests
```
Final once after complete review: `dotnet test tests/SeqDoc.AcceptanceTests/SeqDoc.AcceptanceTests.csproj -c Release --filter "FullyQualifiedName~FixtureCleanupAuthorityTests|FullyQualifiedName~FixtureCleanupProcessTests|FullyQualifiedName~ProcessOwnershipTests"`, exactly `82 passed/0 failed/0 skipped`, recording SDK, Windows/Git/RM capabilities, SHA, and no passing skips. Worker self-review, one latest-head non-author human peer (reserved Abood-essa; replacement only under policy evidence), complete dispositions, then green focused lane and ReviewRequired precede the final gate. Stop for any red, race, malformed interop, missing capability, unsafe authority, path expansion, or inability to amend to accepted GH-116. Shared paths are sequential only. Issues #116–#118 and parent/reviewer comments predate this package; no product/test command, activation, implementation, commit, or push occurred in preparation. Both peers must review this same package commit SHA; no Ready now.

Phase A is the split decision: both Qhatahet and Abood-essa review this immutable planning SHA and post authenticated T2 receipts; that authorizes only split/spec/allowlists, not implementation review or a final gate. Phase B is per-issue: implementer self-review, Reviewer agent, dispositions, focused green, `ReviewRequired`, then one latest-head non-author human approval for the implementation SHA (B2 Abood; replacement only by policy evidence), followed by the final gate. Phase B cannot amend Phase A. Before Ready, mechanically amend the exact accepted GH-116 merge/head and claims and rerun readiness audit; this needs assigned issue readiness approval, not a new split decision unless scope/contract changes.
