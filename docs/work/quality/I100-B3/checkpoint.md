# I100-B3 live cleanup, quarantine, and integration

## State

`Blocked`

This package becomes immutable only at its commit SHA. It is planning-only, with baseline explicitly provisional `18aae5e0364c0b13549bd0c5333ba48f8116bc9a`. Issue [#118](https://github.com/Bilaltariq41/SeqDoc/issues/118) depends on GH-117. Before `Ready`, amend the baseline to the exact accepted GH-117 merge/head and carry handoff claims. No owner authorization is claimed. GH-106 remains pinned at head `182ea35533482cebdfc070b368f3a7fa247a1735` and merge `227d2e9f8b49ce6a414795b16bb0408ed213012a`.

## Scope

Only `FixtureCleanup.cs`, `FixtureCleanupTests.cs`, this capsule/ledger, and generated canonical state through `work_state.py` are initially allowed. `SeqDoc.AcceptanceTests.csproj` requires a new peer-approved amendment after a concrete compiler/build dependency proves existing SDK wildcard inclusion and ProcessOwnership stub staging insufficient. No other project/package/stub/product/workflow or external-source changes. Shared paths use strict sequential leases, never parallel or stacked implementation.

Authored package paths are this checkpoint, ledger, and `docs/project/work-items/GH-118.json`; implementation paths are the two FixtureCleanup files; generated `docs/project/execution.json` is expected unchanged and idle. Current-main planning baseline is exactly `18aae5e0364c0b13549bd0c5333ba48f8116bc9a`; no current-main test or acceptance result is claimed. Prospective activation claims, acquired only after GH-117 claims are released/closed, GH-117 is accepted, and review is complete: `path:tests/seqdoc.acceptancetests/fixturecleanup.cs`, `path:tests/seqdoc.acceptancetests/fixturecleanuptests.cs`, `path:docs/work/quality/i100-b3`, `path:docs/project/work-items/gh-118.json`, `fixture:fixturecleanup-integration`, `governance-tool:tools/governance/work_state.py`, `exclusive:acceptance-git-worktree-metadata`. No csproj claim.

## Frozen integration contract and risks

The caller-authorized control parent is a canonical local-drive directory with captured identity and non-reparse chain. The owned root is direct child `seqdoc-fixture-<token>`; quarantine is its direct sibling `seqdoc-fixture-<token>.quarantine`, same parent/volume, absent before move. Collision refuses without overwrite, merge, or retry. Immediately before atomic `Directory.Move`, revalidate source/control/destination-parent chains, identities, containment, sentinel bytes, roles, active-zero, registration, and admin absence. On success the moved sentinel remains; a same-process local residual receipt records destination path/identity/token for cleanup/report, while stable evidence exposes only role/classification. Result is degraded/non-success. A reconstructed process cannot delete it; only the in-memory owner may remove it after repeated full checks.

The rooted Git executable remains B1's exact admission. The existing staged #106 ProcessOwnership stub has no lock-file mode. Freeze two independent actors: (a) the exact existing assembly-staged rooted stub vector `sleep-with-marker <owned-absolute-marker-path> 30000`, with marker under the receipt-listed output role, proves fixture process-family ownership; (b) the test host independently opens the exact receipt-listed lock target with `FileMode.Open`, `FileAccess.ReadWrite`, `FileShare.None`. A bounded marker observer uses an injected monotonic deadline and filesystem event/poll seam, not an arbitrary sleep, and establishes exact marker bytes parsing to `ProcessId` equal to public `ContainedProcess.ProcessId` before cleanup. An injected cleanup observer/barrier records the first exact delete 32/33 and RM files registration, signals the test host to dispose the FileStream, then releases the barrier. Both sides are bounded by the outer deadline; no `Thread.Sleep` or `Task.Delay` timing synchronization occurs (only production retry sleeper under injected control). The marker must be absent after forced termination and family zero is proven through #106 public API. RM local diagnostics may identify testhost PID+start only and never terminate it. The stub path remains rooted, non-reparse, and FILE_ID checked. Admission failures (Windows/x64, rooted Git/stub, RM DLL/API, disposable repo, handshake) fail tests, never skip. Carry B1 vectors before create/after cleanup.

The concurrency key is canonical common-directory FILE_ID. Only metadata mutation is serialized: deterministic barriers prove same-repository non-overlap and different-repository overlap. All existing risks are ownership replacement, quarantine collision/escape/reparse/cross-volume, live lock attribution, residual safety, stale acceptance count, and scheduler nondeterminism.

## Tests and budget

Add exactly four `[Fact]` methods in `FixtureCleanupIntegrationTests`: (1) quarantine success, collision, replacement, reparse, escape, and reconstructed-owner refusal; (2) live disposable-repository lock handshake, exact RM observation, deterministic release, cleanup, and no residual; (3) same/different repository concurrency and vector preservation; (4) acceptance-project integration admission and failure partitions. Focused filter discovers exactly `4 passed/0 failed/0 skipped`; no passing skip for ten fixture groups. This is the minimum live boundary coverage; no budget exception.

## Verification, review, and stop

Focused (not run in planning):
```powershell
dotnet test tests/SeqDoc.AcceptanceTests/SeqDoc.AcceptanceTests.csproj -c Release --filter FullyQualifiedName~FixtureCleanupIntegrationTests
```
Final affected gate once after review: `dotnet test tests/SeqDoc.AcceptanceTests/SeqDoc.AcceptanceTests.csproj -c Release --filter "FullyQualifiedName~FixtureCleanupAuthorityTests|FullyQualifiedName~FixtureCleanupProcessTests|FullyQualifiedName~FixtureCleanupIntegrationTests|FullyQualifiedName~ProcessOwnershipTests"`, exactly `86 passed/0 failed/0 skipped`. Complete acceptance project command, once after review: `dotnet test tests/SeqDoc.AcceptanceTests/SeqDoc.AcceptanceTests.csproj -c Release`. Before B3 readiness, capture a clean current-main baseline with exact counts/signatures and acceptance outcome, including the ten fixture groups; receipt records SDK/platform/capabilities/SHA/counts. Reviewer route is self-review plus one latest-head non-author human peer reserved to Qhatahet; replacements require policy evidence. Issues #116–#118 and parent/reviewer comments predate this package; no product/test command, activation, implementation, commit, or push occurred in preparation. Both peers must review this same package commit SHA; no Ready now. Stop for any unsafe move, residual registration/admin/root, handshake timeout, capability absence, vector drift, overlap proof failure, red test, or target-path expansion. Close GH-107 only after merge and receipts.

Phase A is the split decision: both Qhatahet and Abood-essa review this immutable planning SHA and post authenticated T2 receipts; that authorizes only split/spec/allowlists, not implementation review or a final gate. Phase B is per-issue: implementer self-review, Reviewer agent, dispositions, focused green, `ReviewRequired`, then one latest-head non-author human approval for the implementation SHA (B3 Qhatahet; replacement only by policy evidence), followed by the final gate. Phase B cannot amend Phase A. Before Ready, mechanically amend the exact accepted GH-117 merge/head and claims and rerun readiness audit; this needs assigned issue readiness approval, not a new split decision unless scope/contract changes.
